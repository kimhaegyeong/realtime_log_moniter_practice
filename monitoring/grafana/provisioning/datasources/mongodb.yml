apiVersion: 1

datasources:
  - name: MongoDB
    type: grafana-mongodb-datasource
    access: proxy
    url: mongodb://mongodb:27017
    database: logs
    jsonData:
      authSource: admin
      tlsSkipVerify: true
    secureJsonData:
      username: admin
      password: admin123
    isDefault: true
    editable: true
